name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package + dev dependencies
        run: |
          python -m pip install --upgrade pip
          # rerandomstats is not on PyPI — install from the v0.2.0 git tag
          # BEFORE the editable install so pip's dependency resolver finds it
          # locally when it processes the `rerandomstats >= 0.2.0` constraint.
          pip install "git+https://github.com/zerotonin/reRandomStats.git@v0.2.0"
          pip install -e ".[dev]"

      - name: Lint with ruff
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
        run: ruff check src tests scripts

      - name: Run tests with coverage
        run: |
          pytest --cov=digimuh --cov-report=xml --cov-report=term -v

      - name: Upload coverage artifact
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.xml
